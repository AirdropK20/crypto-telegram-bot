name: Crypto Telegram Alerts (DEBUG)

on:
  workflow_dispatch:

jobs:
  debug:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Show working directory
        run: |
          echo "=== CURRENT DIRECTORY ==="
          pwd
          echo "=== FILES IN ROOT ==="
          ls -la
          echo "=== SEARCH FOR telegram_news.py ==="
          find . -name "telegram_news.py"
